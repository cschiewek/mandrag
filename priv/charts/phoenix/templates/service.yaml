apiVersion: v1
kind: Service
metadata:
  name: {{ template "phoenix.fullname" . }}
  labels:
    app: {{ template "phoenix.name" . }}
    chart: {{ template "phoenix.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "phoenix.name" . }}
    release: {{ .Release.Name }}
