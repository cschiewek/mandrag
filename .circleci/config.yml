# Elixir CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-elixir/ for more details
version: 2
jobs:
  build:
    docker:
      - image: cschiewek/alpine-elixir:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run: mix deps.get
      - run: mix test
